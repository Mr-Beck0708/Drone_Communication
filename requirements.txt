# Cryptographic Libraries
cryptography>=41.0.0
pycryptodome>=3.19.0

# Post-Quantum Cryptography
# Note: liboqs-python 0.14.1 has a known installation bug on Windows
# The library will use fallback dummy implementations if liboqs fails to install
liboqs-python>=0.10.0

# Networking
asyncio>=3.4.3
aiohttp>=3.9.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Benchmarking
memory-profiler>=0.61.0
psutil>=5.9.0

# Logging and Monitoring
structlog>=23.2.0

# Configuration
pyyaml>=6.0.1
